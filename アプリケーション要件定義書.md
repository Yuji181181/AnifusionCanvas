# **アプリケーション開発用詳細要件定義書**

## **開発の背景と目的**

アニメーション制作産業は世界的な需要の拡大を背景に成長を続けているが、その制作現場は歴史的に極めて労働集約的なプロセスに依存している。特に、原画と原画の間を補間して滑らかな動きを作り出す「中割り」の工程は、膨大な作画枚数を必要とし、制作スケジュールの遅延やクリエイターの過重労働を引き起こす最大のボトルネックとなっている。

これまでの映像制作業界では、実写映像向けのフレーム補間技術を用いた自動化が試みられてきたが、アニメーションのフレームは時間的に疎（Sparse）であり、非線形な大きな動きや隠蔽（オクルージョン）を伴うため、従来技術では不自然な破綻が必然的に生じていた。

本プロジェクトは、これらの課題を解決する「アニメ制作DX」を実現するためのプロトタイプ（デモ）開発である。最新の生成AI技術とモダンなWebフロントエンド技術を統合した「Human-in-the-Loop（人間とAIの協調）」のワークフローを構築し、現場のクリエイターが実務で利用可能なレベルの支援ツールを提案する。

また、本デモアプリケーションの開発は、エンターテインメントの基盤と高度な技術力を併せ持つ企業に対する、開発者の強い熱意とフルスタックなAIエンジニアリング能力をアピールするためのポートフォリオとして機能することを最大の目的としている。

## **推奨OSS選定とアーキテクチャ**

個人開発において、全ての機能を一から実装することは非現実的である。本プロジェクトでは、各機能領域で最適なOSSを選定し、それらを適切に組み合わせることで、短い開発期間でも高品質なアプリケーションを実現する。

### **フロントエンド構成**

| 機能領域 | 推奨OSS | 選定理由 |
|---------|---------|----------|
| **UIコンポーネント** | **shadcn/ui + Radix UI** | アクセシビリティ対応済み、TailwindCSSとの親和性、カスタマイズ性が高い |
| **状態管理** | **Zustand** | シンプルなAPI、TypeScriptネイティブ、小規模〜中規模に最適 |
| **データフェッチ** | **useSWR** | Next.js推奨、キャッシュ・再検証・ポーリング機能が充実 |
| **フォーム管理** | **React Hook Form + Zod** | パフォーマンス優秀、型安全性あり |

### **Canvas編集とAI機能の分離**

本アプリケーションの最大の設計上の判断は、**Canvas編集機能とAI推論機能を明確に分離すること**である。

**Fabric.jsの役割（純粋なCanvas編集）:**
- ステップ3「手動編集」でのみ使用
- 機能: ペン描画、図形、フィルター、ピクセル単位編集
- Fabric.jsにAI推論ロジックを混在させない

**shadcn/ui + カスタム実装（AIワークフローUI）:**
- ステップ1「AI中割り生成」: フォームUI、プレビュー
- ステップ2「Inpainting編集」: マスク描画用Canvas（Fabric.jsの最小機能）+ プロンプト入力フォーム

### **テスト戦略**

品質担保のため、以下のテストアプローチを採用する：

**単体・統合テスト: Vitest**
- フロントエンドのユニットテストフレームワークとして **Vitest** を採用
- Jestと互換性があり、ESMネイティブ、TypeScriptサポートが優秀
- Reactコンポーネント、カスタムフック、ユーティリティ関数のテスト
- FastAPIとのAPI通信部分のモックテスト

**E2Eテスト: Playwright**
- **Playwright** をE2Eテストフレームワークとして採用
- Chromium, Firefox, WebKitのマルチブラウザサポート
- 主要ユーザージャーニーの自動化:
  - 中割り生成 → Inpainting編集 → 手動編集 → 動画書き出し
- ヘッドレス実行でのCI/CD統合

**テスト実装方針**
- ビジュアルリグレッションテストはスコープ外（ポートフォリオ用途のデモのため）
- 重要なビジネスロジック（フレーム管理、API通信）はユニットテストでカバー
- クリティカルパス（3ステップのワークフロー）はE2Eテストで保証

## **システムアーキテクチャおよび技術スタック**

本アプリケーションは、運用コストの最適化とデモとしての軽快な動作を両立するため、フロントエンドとバックエンドの責務を完全に分離したアーキテクチャを採用する。以下の技術スタックを厳守して実装すること。

| コンポーネント / 領域 | 指定技術および制約 |
| :---- | :---- |
| **フロントエンド基盤** | **Next.js (App Router)** 実行環境およびパッケージマネージャーには **bun** を使用すること。 |
| **データフェッチ・通信** | Next.jsのバックエンド機能（Route HandlersやServer Actionsなど）は基本使用せず、純粋なフロントエンドアプリケーションとして構築する。バックエンドのFastAPIサーバーへ直接通信を行い、APIのポーリングやデータ取得には **useSWR** を使用する。 |
| **スタイリング** | **TailwindCSS** を使用してモダンでレスポンシブなUIを構築する。 |
| **バリデーション** | **Zod** を使用し、フロントエンドの入力検証およびAPIのリクエスト/レスポンスの型安全性を担保する。 |
| **コード品質管理** | リンターおよびフォーマッタとして **Biome** を採用する。 |
| **状態管理** | **Zustand** を使用してフレーム管理などのグローバル状態を管理する。 |
| **画像編集・UI** | **Fabric.js** を用いてキャンバスを実装し、ステップ2（マスク描画）とステップ3（フル機能編集）で使用する。 |
| **バックエンド・API基盤** | **FastAPI** を使用。 Next.js側でバックエンド処理を持たないため、データベースとの接続、非同期ジョブの管理、および外部ストレージ連携などのビジネスロジックはすべてFastAPI側に集約する。 ※APIエンドポイントの具体的な設計は実装担当AIエージェントに委ねる。 |
| **Pythonパッケージ管理** | **uv** を使用する。 依存ライブラリの追加には pip や uv pip install ではなく、必ず **uv add** コマンドを使用し、モダンなプロジェクト管理を行うこと。 |
| **オブジェクトストレージ** | **Cloudflare R2**（アップロードにはFastAPI経由、または署名付きURLを用いたダイレクトアップロード等を採用）。 |
| **データベース** | **TiDB Serverless** ※テーブルのスキーマ構造や、Python側でのORM選定などの具体的な実装内容は実装担当AIエージェントに委ねる。 |
| **GPU推論基盤** | **Modal.com** 等のクラウドGPU、またはローカル環境（FastAPIをホストし、バックグラウンドタスクとしてAI推論を実行）。 |
| **単体・統合テスト** | **Vitest** - Reactコンポーネント、カスタムフック、ユーティリティ関数のテスト。FastAPIとのAPI通信部分のモックテスト。 |
| **E2Eテスト** | **Playwright** - ユーザージャーニーの自動化、マルチブラウザサポート（Chromium, Firefox, WebKit）。 |

### **全体アーキテクチャ図**

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           クライアント層 (Next.js)                        │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                     ページ/ルーティング                             │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐            │  │
│  │  │ /step1   │  │ /step2   │  │ /step3   │  │ /export  │            │  │
│  │  │ 中割り生成 │  │ Inpainting│  │ 手動編集  │  │ 動画書き出し│            │  │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘            │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                     共通コンポーネント層                          │  │
│  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌────────────┐ │  │
│  │  │ Timeline    │ │ FrameCard   │ │ UploadForm  │ │ Preview    │ │  │
│  │  │ (タイムライン)│ │ (フレーム表示)│ │ (アップロード)│ │ (プレビュー) │ │  │
│  │  └─────────────┘ └─────────────┘ └─────────────┘ └────────────┘ │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                     状態管理層 (Zustand)                          │  │
│  │  ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────────┐ │  │
│  │  │ useFrameStore   │ │ useJobStore       │ │ useEditorStore      │ │  │
│  │  │ (フレーム管理)   │ │ (ジョブ状態管理)  │ │ (エディタ状態)      │ │  │
│  │  └─────────────────┘ └─────────────────┘ └─────────────────────┘ │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                     API通信層 (useSWR)                           │  │
│  │  ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────────┐ │  │
│  │  │ api/inference   │ │ api/frames        │ │ api/export          │ │  │
│  │  │ (推論ジョブ)     │ │ (フレーム管理)    │ │ (書き出し)          │ │  │
│  │  └─────────────────┘ └─────────────────┘ └─────────────────────┘ │  │
│  └─────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
                                      │
                                      │ HTTP/REST
                                      ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                          API層 (FastAPI)                                  │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                     ルーティング層                               │   │
│  │  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐            │   │
│  │  │ /inference   │ │ /frames      │ │ /export      │            │   │
│  │  │ (推論エンドポイント)│ │ (フレーム管理) │ │ (書き出し)     │            │   │
│  │  └──────────────┘ └──────────────┘ └──────────────┘            │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                     サービス層 (ビジネスロジック)                 │   │
│  │  ┌────────────────┐ ┌────────────────┐ ┌────────────────┐          │   │
│  │  │ InferenceService│ │ FrameService   │ │ ExportService  │          │   │
│  │  │ (推論管理)      │ │ (フレーム管理)  │ │ (動画エンコード)│          │   │
│  │  └────────────────┘ └────────────────┘ └────────────────┘          │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                     ジョブキュー層 (Celery/Redis)                │   │
│  │  ┌────────────────┐ ┌────────────────┐ ┌────────────────┐          │   │
│  │  │ tooncrafter_job │ │ inpainting_job │ │ export_job     │          │   │
│  │  │ (中割り生成)    │ │ (部分修正)     │ │ (動画書き出し) │          │   │
│  │  └────────────────┘ └────────────────┘ └────────────────┘          │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                      │
                                      │ SQL/TiDB
                                      ▼
                              ┌───────────────┐
                              │  TiDB Serverless │
                              │  (メタデータ管理)  │
                              │  - users        │
                              │  - projects     │
                              │  - frames       │
                              │  - jobs         │
                              └───────────────┘
                                      │
                                      │ S3 API
                                      ▼
                              ┌───────────────┐
                              │ Cloudflare R2  │
                              │ (画像ストレージ) │
                              │ - uploads/     │
                              │ - generated/   │
                              │ - exports/     │
                              └───────────────┘
```

### **ディレクトリ構成**

```
anifusion-canvas/
├── .claude/                      # Claude Code設定
├── .github/                      # GitHub Actions等
├── apps/                         # アプリケーション層
│   └── web/                      # Next.jsアプリケーション
│       ├── .next/                # ビルド出力
│       ├── app/                  # Next.js App Router
│       │   ├── layout.tsx        # ルートレイアウト
│       │   ├── page.tsx          # トップページ
│       │   ├── step1/            # ステップ1: 中割り生成
│       │   │   └── page.tsx
│       │   ├── step2/            # ステップ2: Inpainting
│       │   │   └── page.tsx
│       │   ├── step3/            # ステップ3: 手動編集
│       │   │   └── page.tsx
│       │   └── export/           # 動画書き出し
│       │       └── page.tsx
│       ├── components/           # 共通コンポーネント
│       │   ├── ui/               # shadcn/uiコンポーネント
│       │   ├── timeline/         # タイムライン関連
│       │   ├── editor/           # エディタ関連
│       │   └── forms/            # フォーム関連
│       ├── hooks/                # カスタムフック
│       │   ├── use-frames.ts
│       │   ├── use-jobs.ts
│       │   └── use-canvas.ts
│       ├── lib/                  # ユーティリティ
│       │   ├── api.ts            # APIクライアント
│       │   ├── utils.ts
│       │   └── constants.ts
│       ├── stores/               # Zustandストア
│       │   ├── frame-store.ts
│       │   ├── job-store.ts
│       │   └── editor-store.ts
│       ├── types/                # TypeScript型定義
│       │   ├── frame.ts
│       │   ├── job.ts
│       │   └── api.ts
│       ├── tests/                # テストファイル
│       │   ├── unit/
│       │   └── e2e/
│       ├── public/               # 静的ファイル
│       ├── styles/               # グローバルスタイル
│       ├── next.config.js
│       ├── tailwind.config.ts
│       ├── tsconfig.json
│       ├── biome.json
│       ├── vitest.config.ts
│       ├── playwright.config.ts
│       └── package.json
│
├── apps/api/                     # FastAPIバックエンド
│   ├── app/                      # アプリケーションコード
│   │   ├── __init__.py
│   │   ├── main.py               # FastAPIエントリーポイント
│   │   ├── config.py             # 設定管理
│   │   ├── dependencies.py       # DIコンテナ
│   │   ├── routers/              # APIルーター
│   │   │   ├── __init__.py
│   │   │   ├── inference.py      # 推論エンドポイント
│   │   │   ├── frames.py         # フレーム管理
│   │   │   └── export.py         # 書き出しエンドポイント
│   │   ├── services/             # ビジネスロジック
│   │   │   ├── __init__.py
│   │   │   ├── inference.py      # 推論サービス
│   │   │   ├── frame.py          # フレームサービス
│   │   │   └── export.py         # エクスポートサービス
│   │   ├── models/               # データモデル
│   │   │   ├── __init__.py
│   │   │   ├── frame.py
│   │   │   ├── job.py
│   │   │   └── project.py
│   │   ├── schemas/              # Pydanticスキーマ
│   │   │   ├── __init__.py
│   │   │   ├── frame.py
│   │   │   ├── job.py
│   │   │   └── inference.py
│   │   ├── infrastructure/         # インフラ層
│   │   │   ├── __init__.py
│   │   │   ├── database.py       # TiDB接続
│   │   │   ├── storage.py        # R2接続
│   │   │   ├── cache.py          # Redis接続
│   │   │   └── message_queue.py  # Celery設定
│   │   └── workers/              # バックグラウンドワーカー
│   │       ├── __init__.py
│   │       ├── celery_app.py
│   │       ├── tasks/            # Celeryタスク
│   │       │   ├── __init__.py
│   │       │   ├── tooncrafter.py
│   │       │   ├── inpainting.py
│   │       │   └── export.py
│   │       └── ai_models/        # AIモデルラッパー
│   │           ├── __init__.py
│   │           ├── base.py
│   │           ├── tooncrafter.py
│   │           ├── inpainting.py
│   │           └── lora.py
│   ├── tests/                    # テスト
│   │   ├── __init__.py
│   │   ├── unit/
│   │   ├── integration/
│   │   └── conftest.py
│   ├── alembic/                  # DBマイグレーション
│   │   └── versions/
│   ├── scripts/                  # スクリプト
│   │   ├── setup.sh
│   │   └── download_models.sh
│   ├── .env.example
│   ├── .env.local
│   ├── pyproject.toml
│   ├── uv.lock
│   ├── alembic.ini
│   └── Dockerfile
│
├── packages/                     # 共有パッケージ（モノレ構成時）
│   └── types/                    # 共有型定義
│       ├── package.json
│       └── src/
│           ├── index.ts
│           ├── frame.ts
│           └── api.ts
│
├── infra/                        # インフラ設定
│   ├── terraform/                # Terraform設定（オプション）
│   └── docker/                   # Docker Compose設定
│       └── docker-compose.yml
│
├── docs/                         # ドキュメント
│   ├── architecture/
│   ├── api/
│   └── setup.md
│
├── .github/                      # GitHub設定
│   └── workflows/
│       ├── ci.yml
│       └── deploy.yml
│
├── .gitignore
├── .editorconfig
├── README.md
└── package.json                  # ルートpackage.json（ワークスペース設定）
```

## **インタラクティブな編集ワークフロー（UI要件）**

本アプリケーションのコアとなるユーザー体験は、AIにすべてを任せるのではなく、クリエイターが介在してクオリティを担保する以下の3ステップのフローで構成される。フロントエンドはこのフローを直感的に操作できるUIを提供すること。

### **ステップ1: AIで中割りを生成**

ユーザーが2枚の原画（キーフレーム）をアップロードし、生成したい枚数を指定すると、バックエンドのAIがその間のフレームを自動補間し、個別のフレーム画像としてタイムラインに並べる。

**使用技術:**
- UI: shadcn/ui（フォーム、ボタン、アップロード）
- 状態管理: Zustand（アップロード進捗、生成ジョブ状態）
- 通信: useSWR（FastAPIへのポーリング）
- 表示: 生成されたフレームをタイムラインコンポーネントで表示

### **ステップ2: AIで、その生成した画像に対して自然言語で指示をして修正**

生成された中割り画像の中で破綻している部分（例：手の形がおかしい等）があった場合、ユーザーはエディタのキャンバス上でそのフレームだけをブラシでマスク（黒塗り）する。その後、「握りこぶし」などの自然言語によるプロンプトを入力して再生成を指示することで、AIがそのマスク部分だけを修正（Inpainting）する。

**使用技術:**
- マスク描画: Fabric.js（ブラシ機能のみ使用、最小限の実装）
- プロンプト入力: shadcn/ui（テキストエリア、ボタン）
- 通信: useSWR（マスク画像とプロンプトをFastAPIへ送信、結果をポーリング）
- 状態管理: Zustand（編集中のフレーム状態）

**Fabric.jsの使用範囲（最小限）:**
```javascript
// マスク描画のみに限定
const maskCanvas = new fabric.Canvas('maskCanvas', {
  isDrawingMode: true,
  width: 512,
  height: 512,
});
maskCanvas.freeDrawingBrush = new fabric.PencilBrush(maskCanvas);
maskCanvas.freeDrawingBrush.color = 'black';
maskCanvas.freeDrawingBrush.width = 20;
```

### **ステップ3: それでも修正できなかった部分を、人間の手で直接編集**

AIの自然言語修正でもクリエイターの意図に完璧に合致しない1ピクセル単位のズレや色の修正については、キャンバスエディタ上の描画ツールを用いて、人間の手で直接ピクセルを編集・加筆して最終的な完成とする。

**使用技術:**
- キャンバスエディタ: Fabric.js（フル機能）
- 機能一覧:
  - ペンツール（自由描画、線の太さ・色変更）
  - 図形ツール（四角、円、多角形）
  - テキストツール
  - 画像フィルター（明度、コントラスト、彩度、ブラー）
  - 選択・変形（移動、回転、スケール）
  - レイヤー管理（重ね順、表示/非表示）
  - アンドゥ/リドゥ

**Fabric.jsのフル活用:**
```javascript
const editorCanvas = new fabric.Canvas('editorCanvas', {
  width: 1024,
  height: 1024,
  backgroundColor: '#f0f0f0',
});

// 画像読み込み
fabric.Image.fromURL(frameImageUrl, (img) => {
  editorCanvas.add(img);
  editorCanvas.renderAll();
});

// フィルター適用
const applyFilter = (filterType, value) => {
  const activeObject = editorCanvas.getActiveObject();
  if (activeObject && activeObject.type === 'image') {
    activeObject.filters.push(new fabric.Image.filters[filterType]({ value }));
    activeObject.applyFilters();
    editorCanvas.renderAll();
  }
};
```

**使用技術:**
- キャンバスエディタ: Fabric.js（フル機能）
- 機能一覧:
  - ペンツール（自由描画、線の太さ・色変更）
  - 図形ツール（四角、円、多角形）
  - テキストツール
  - 画像フィルター（明度、コントラスト、彩度、ブラー）
  - 選択・変形（移動、回転、スケール）
  - レイヤー管理（重ね順、表示/非表示）
  - アンドゥ/リドゥ

**Fabric.jsのフル活用:**
```javascript
const editorCanvas = new fabric.Canvas('editorCanvas', {
  width: 1024,
  height: 1024,
  backgroundColor: '#f0f0f0',
});

// 画像読み込み
fabric.Image.fromURL(frameImageUrl, (img) => {
  editorCanvas.add(img);
  editorCanvas.renderAll();
});

// フィルター適用
const applyFilter = (filterType, value) => {
  const activeObject = editorCanvas.getActiveObject();
  if (activeObject && activeObject.type === 'image') {
    activeObject.filters.push(new fabric.Image.filters[filterType]({ value }));
    activeObject.applyFilters();
    editorCanvas.renderAll();
  }
};
```

### **ステップ4: 動画化は最後に手動で実行**

細部の修正が終わったあと、ユーザーが明示的に書き出しボタンを押した場合のみ、フレーム列をFFmpegでMP4に結合する。自動再生用のプレビュー生成と、最終書き出しは分離する。

**使用技術:**
- プレビュー: HTML5 Video要素（フレーム画像を順番に表示する簡易プレビュー）
- 書き出し: FastAPI経由でFFmpeg実行
- 通信: useSWR（書き出しジョブのポーリング）

**使用技術:**
- プレビュー: HTML5 Video要素（フレーム画像を順番に表示する簡易プレビュー）
- 書き出し: FastAPI経由でFFmpeg実行
- 通信: useSWR（書き出しジョブのポーリング）

## **AIモデルの選定と推論要件**

デモの品質と実装の現実性を両立するため、FastAPIバックエンドで稼働するAI推論パイプラインは以下の戦略に基づいて設計する。

* **中割り生成モデル:** アニメーションのフレーム補間に特化したOSSモデルである **ToonCrafter** を採用する。  
* **部分修正（Inpainting）モデル:** ステップ2の自然言語による修正機能には、**Stable Diffusion v1.5系 または SDXL の Inpainting特化モデル**（アニメ画風に強いモデル、例：AnyLoRA等）を利用する。  
* **キャラクター制御（LoRA適用）:** 別のタスクとして事前学習済みのキャラクターLoRAモデルが用意される想定である。推論時にこのLoRAの重みをベースモデルに適用し、キャラクターのアイデンティティを保った画像を生成できるように実装すること。  
* **IP-Adapter（オプション）:** ユーザーが設定画（リファレンス画像）をアップロードし、その画像特徴量をプロンプトとして直接モデルに注入する機構の導入も推奨する。

## **バックエンド推論処理と非同期ジョブ制御の要件**

長時間を要するAI推論ワークロードを安定して処理するため、FastAPIとNext.jsの間で適切な非同期通信パターンを構築する。

* **ジョブキューとポーリング:**  
  Next.jsからFastAPIへ推論をリクエストした際、FastAPIは即座にジョブIDのみを返却し、裏側（バックグラウンド）で重い推論タスクを実行する。フロントエンドは **useSWR** を用いてFastAPIのエンドポイントを定期的にポーリングし、ジョブの完了ステータスと生成された画像のURLを取得する構成とすること。  
* **GPUメモリの最適化:**  
  ToonCrafter等のモデルはVRAM消費が激しいため、FP16（半精度浮動小数点）でのロードや、不要なモデルのCPUへのオフロードなど、VRAM使用量を抑える工夫をPythonコード内に施すこと。  
* **動画のエンコーディング (FFmpeg):**  
  生成された複数枚のフレームは、まず個別画像として保持する。フレームの細かい編集が終わったあと、ユーザーが手動で書き出しを押した時だけMP4動画として結合する。本アプリケーションはポートフォリオ用のデモであるため、厳密な24fpsや30fpsにこだわる必要はない。ToonCrafterの出力仕様や実装の容易さを最優先とし、最も実装が簡単でエラーが起きにくいフレームレートを実装担当AIが選択してFFmpegのエンコード設定を行うこと。

実装の細部（DBスキーマやAPI仕様など）は本要件定義書の意図を汲み取り、実装を担当するAIエージェントが最も効率的で堅牢な設計を自ら考え、コードとして具現化すること。